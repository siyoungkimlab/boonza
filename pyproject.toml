[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "boonza"
dynamic = ["version"]
description = "A molecular reader, editor and writer that helps humans and AI understand how atoms and molecules are arranged in space"
readme = "README.md"
# MIT, except the three modules that port LGPL-2.1 code (secondary.py,
# matchmaker.py, shrake_rupley.py); see NOTICE
license = "MIT AND LGPL-2.1-only"
license-files = ["LICENSE", "LICENSES/*", "NOTICE"]
requires-python = ">=3.11"
dependencies = [
    # 2.4 and later need x86-64-v2 CPUs, which older HPC nodes lack
    "numpy>=2.0,<2.4",
    "numba>=0.59",
    "rdkit>=2023.9",
    "openmm>=8.1",
    "pandas>=2.0",
    "networkx>=3.0",
]

[project.optional-dependencies]
# development: tests (with MDAnalysis as a reference) and lint
dev = ["pytest", "ruff", "MDAnalysis>=2.7"]

[project.scripts]
boonza = "boonza.cli:main"

[tool.setuptools.dynamic]
version = { attr = "boonza.__version__" }

[tool.setuptools.package-data]
boonza = ["data/*.dms", "data/*.zip"]

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "B", "UP"]

[tool.pytest.ini_options]
testpaths = ["tests"]
